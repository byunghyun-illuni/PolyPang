# Nixpacks 설정 - Railway 배포용
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = [
  "cd client && npm ci --include=dev && npm run build",
  "cd server && npm ci --include=dev && npm run build"
]

[start]
cmd = "cd server && npm start"

